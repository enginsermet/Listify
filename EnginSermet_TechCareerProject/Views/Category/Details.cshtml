@model EnginSermet_TechCareerProject.Entities.Category

@{
    ViewData["Title"] = "Details";
    var base64 = Convert.ToBase64String(ViewBag.Picture);
    var imgsrc = String.Format("data:image/gif;base64,{0}", base64);
}

<h1>@ViewBag.CatehogyName</h1>
<hr />

<div style="display: flex;">
    <div class="col-6">
        <img src="@imgsrc" width="400" height="400">
    </div>

    <div class="col-6">
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Description)
            </dd>
        </dl>
        <a asp-action="Edit" asp-route-id="@Model?.CategoryId">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
</div>

